# T01 — P4A 现网实测（经 prefix-cache 与代理优化后的批次）

**可信度**：自有实测
**Source**: `experiments/p4a/refractor.md:10-19`（表体）与 `:250-256`（附录口径）
**支撑**: C01

## 表

| 指标 | 实测值（中位） |
|---|---|
| 单篇会话耗时 | 144 s（全周期 172.5 s） |
| 工具调用 | 26 次 / 16 步 |
| 累计计费 input | 约 140 万 token |
| 最终一步真实上下文 | 仅 8–11 万 token |
| output | 1.1 万 token |
| 吞吐 | 19.7 篇/h（串行，GPU 占空比约 52%） |

## 采样口径（关键——决定这些数字能支撑多强的结论）

| 指标 | 样本 | 窗口 |
|---|---|---|
| 吞吐 / 耗时 | 295 篇 | 2026-07-08 优化后的 15 小时窗口 |
| token | 抽样 **80 篇** 的 `agent_usage.json` | — |
| 放大倍数（10–18×） | **仅 3 个会话**的逐事件解剖 | — |
| 论文规模 | 抽样 40 篇 Markdown：p50 86KB(≈21K token) / p90 147KB(≈37K token) | — |
| vLLM | `qwen3.6-35b-a3b`，max_model_len 262144，prefix cache 命中率 89%，KV 占用 2.8%，0 抢占 | — |

> **注意样本量**：10–18× 这个区间来自 **3 个会话**。T02 的独立复现（第 4 个会话）落在区间内，
> 但这仍是 4 个样本。E01 阶段 2 应当把它扩到全量分布。

## 归因（原文）

> 慢和贵的根因不是某个工具（外部核验单次仅几百 token、几秒），而是 **ReAct 循环每步重发
> 全部历史**：input 计费被放大 10–18 倍；且 agent 的多轮性对最终产出贡献有限——实测 agent
> 最终也是一次 `Write` 写出完整 `agent_judgment.json`，之后只做定点小修。
>
> —— `experiments/p4a/refractor.md:21`
